import { AllRoutes } from "../@types/components/App/routes.type";

const routes: AllRoutes[] = [
    {
        type: 'link',
        key: 'home',
        name: 'home',
        route: '/'
    }
]

export default routes